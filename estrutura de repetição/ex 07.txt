algoritmo "semnome"
var
   contagem, n, soma: inteiro
inicio
      contagem := 1
      soma := 0
      enquanto (contagem <= 10) faca
               escreva(" digite o numero",contagem,"° número : ")
               leia(n)
               contagem := contagem + 1
               soma := soma + n
      fimenquanto
      escreval(" a soma entre todos os números digitados é ",soma)
fimalgoritmo
