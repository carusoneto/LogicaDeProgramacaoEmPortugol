algoritmo "semnome"
var
   contagem, n : inteiro
inicio
      contagem := 1
      enquanto (contagem <= 10) faca
               escreva(" digite o numero",contagem,"° número : ")
               leia(n)
               contagem := contagem + 1
      fimenquanto
fimalgoritmo
