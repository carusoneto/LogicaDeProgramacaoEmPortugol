algoritmo "semnome"
var
   v : vetor [1..5] de inteiro
   c, par : inteiro
inicio
      escreval(" escreva 5 números aleatórios : ")
      para c := 1 ate 5 passo 1 faca
           leia(v[c])
           se ( v[c] % 2 = 0 ) entao
              par := par + 1
           fimse
      fimpara
      limpatela
      escreval(" no intervalo de números digitados ")
      escreval(" foram digitados ",par," números pares, são eles : ")
      para c := 1 ate 5 passo 1 faca
           se ( v[c] % 2 = 0 ) entao
              escreval(" o número ",v[c]," que está na ",c,"° posição")
           fimse
      fimpara
fimalgoritmo